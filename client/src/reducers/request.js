const request = (state = "", action) => {
    switch(action.type){
        case 'UPDATERE':
            return state = action.payload;
        
        default:
            return state 

    }
};

 

export default request;

