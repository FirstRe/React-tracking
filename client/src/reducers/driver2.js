const driver2 = (state = [], action) => {
    switch(action.type){
        case 'UPDATEDRIVER2':
            return state = action.payload;
        
        default:
            return state 

    }
};

 

export default driver2;

